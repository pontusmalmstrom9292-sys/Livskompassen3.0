[project]
name = "livskompassen-ai-scripts"
version = "0.1.1"
requires-python = ">=3.11"
dependencies = []
# google-cloud-aiplatform / vertexai REMOVED 2026-07-18 — Vector Search decommissioned.
# RAG = Firestore Native (functions/) + @google/genai.
