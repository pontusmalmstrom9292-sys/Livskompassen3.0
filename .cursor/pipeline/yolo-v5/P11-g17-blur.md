# P11 — G17 Zero Footprint blur

```
YOLO P11 — G17 blur Zero Footprint (eval-item).

Implementera blur/clear av känslig UI vid tab-byte/logout enligt Zero Footprint-kanon.
Läs GAP-register för G17-status.

Smoke: npm run smoke:locked-ux && npm run smoke:widgets
PMIR-stopp: ingen App Check Enforce, inga rules-ändringar.

Efter kod: notera i docs/SMOKE_RESULTS.md att USER smoke (Fyren efter tab-byte) kräver Pontus.
Jämför mot hela projektets kontext. Arbeta autonomt tills agent-smoke PASS.
```
