---
description: Locked product icons D1 M2 WH1 WH2 — B1 app icon unlocked
alwaysApply: false
globs: src/**/LivskompassMark.tsx,src/**/LivskompassBrandLockup.tsx,src/**/KompisMark.tsx,src/**/FyrenShortcut*.tsx,public/favicon.svg,public/drawer-*.svg,public/wh-*.svg
---

# Locked icons (MUST NOT replace)

Canonical: [`.context/locked-icons.md`](../../.context/locked-icons.md)

| ID | File | Status |
|----|------|--------|
| ~~B1~~ | `public/favicon.svg` | **Upplåst** — app/favicon/Android får bytas (P1–P5 eller ny) |
| D1 | `LivskompassMark.tsx` + `LivskompassBrandLockup.tsx` (Gold stack) | LÅST |
| M2 | `src/modules/kompis/components/KompisMark.tsx` | LÅST |
| WH1 | `FyrenShortcutMicIcon.tsx` + `drawer-inspelning.svg` + `wh-inspelning.svg` | LÅST |
| WH2 | `FyrenShortcutNoteIcon.tsx` + `drawer-anteckning.svg` + `wh-anteckning.svg` | LÅST |

## MUST

- Run `npm run smoke:locked-icons` when touching D1, M2, WH1, or WH2.
- New chrome icons follow [`docs/design/ICON-STYLE-GUIDE.md`](../../docs/design/ICON-STYLE-GUIDE.md) (D1/M2 — **not** B1 Kanon ros as template).

## MUST NOT

- Revert D1 to minimal line-only `LivskompassMark` without user approval.
- Use `LivskompassMark` inside `KompisAvatar` (use `KompisMark` only).
- Revert WH1/WH2 to identical dagbok-placeholder or `shortcutSrc` wh-inspelning img i `FyrenWidgetBar` without user approval.

## App icon (B1 upplåst)

- Telefon/PWA/favicon: [`docs/design/themes/phone-icon-variants/PREVIEW.md`](../../docs/design/themes/phone-icon-variants/PREVIEW.md)
- Android: `npm run android:icons:phone -- <png-1024>`
