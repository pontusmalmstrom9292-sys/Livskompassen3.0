# Worktree multi-agent guide

npm run pipeline:worktree -- flow_<id>

Agents: master-architect, adk-weaver, ux-guardian, security-auditor, verifier.

Merge after pipeline:validate + pipeline:run-smoke PASS.
