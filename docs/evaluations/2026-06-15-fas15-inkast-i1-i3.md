# Fas 15 — Inkast I1/I3 (minimal) — 2026-06-15

**Status:** PASS

**Källor:** [`2026-05-31-fas5c-inkorg-beslut.md`](./2026-05-31-fas5c-inkorg-beslut.md) · [`2026-06-15-fas14-chat3-adk-15.md`](./2026-06-15-fas14-chat3-adk-15.md) · [`.context/domän-covert-narcissism.md`](../../.context/domän-covert-narcissism.md)

---

## Beslut (minimal scope)

| ID | Fas 5C | Fas 15 leverans |
|----|--------|-----------------|
| **I1** MåBra frågekort | DEFER (superseded av `DagligMixPanel` + `KbtTransformatorPanel`) | **Routing only:** `CaptureSuperModule variant="mabra"` → `sourceModule: mabra_inkast` → heuristik `dagbok` → `journal` efter HITL. Ingen ny REFLECTION-bank. |
| **I3** Barnen `LivsloggQuestionCard` | DEFER (locked `BarnfokusFraganPanel`) | **Routing only:** `CaptureSuperModule variant="familjen"` → `sourceModule: familjen` → heuristik `barnen` → `children_logs` efter HITL. Frågepool redan i `BARNFOKUS_QUESTIONS_BUILTIN` — ingen dublett-UI. |

---

## Kodändringar

| Fil | Ändring |
|-----|---------|
| `CaptureSuperModule.tsx` | Ny variant `familjen` + `SOURCE_MODULE.familjen` |
| `FamiljenInkastDelegate.tsx` | Typad `variant="familjen"` (bort med `as any`) |
| `captureDomainCopy.ts` | Domän-hint för `familjen` / `barnfokus` / `barnen` |
| `smoke_inkast_lockdown.mjs` | Statisk + callable preview för I1/I3 |
| `smoke_inbox_sort.mjs` | Preview barnen + mabra_inkast |
| `smoke_locked_ux.mjs` | Assert familjen/mabra inkast-wire |

**Ej rört:** `firestore.rules`, `navTruth`, ekonomi, arbetsliv.

---

## Säkerhet (verifierat)

| Regel | Bevis |
|-------|-------|
| Tre silos (U1) | `barnen` → `children_logs`; `mabra_inkast` → `journal` — ingen cross-RAG |
| DCAP före LLM | `heuristicInboxClassify` matchar `sourceModule` före Gemini |
| WORM | `inboxPersist` + `assertServerWormPayload` oförändrat |
| Ingen auto-promote barn→Valv | `InkastBarnenValvBridge` / `SaveAsEvidencePrompt` opt-in only |
| HITL | `requiresHumanReview` + granskningskö för känslig routing |

---

## Smoke

| Script | Resultat |
|--------|----------|
| `npm run smoke:inkast` | **PASS** |
| `npm run smoke:inbox` | **PASS** |
| `npm run smoke:locked-ux` | **PASS** (UI rörd) |

---

## MUST NOT (bekräftat)

- Inga nya frågekort utan `bankId` / U6 dirigent
- Ingen `LivsloggQuestionCard` / `DailyQuestionCard` port
- Ingen ändring av locked `BarnfokusFraganPanel`

---

## Deploy

Backend oförändrad (heuristik fanns redan i `inboxClassifier.ts`). Frontend:

```bash
npm run build
firebase deploy --only hosting
```
