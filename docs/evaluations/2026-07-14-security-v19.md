# Security audit v19 — read-only

**Datum:** 2026-07-14  
**Agent:** marathon-security (read-only)  
**Fas:** P146

## Smokes

| Smoke | Resultat |
|-------|----------|
| `smoke:manifest` | **PASS** — silo K/V/F, WORM alignment, cross-RAG blocked |
| `smoke:valv-security` | **PASS** — session lifecycle, WORM hardening |

### Nyckelutdata

```
[smoke:manifest] validateSiloIsolationManifest: OK (K/V/F)
[smoke:manifest] WORM sample alignment: OK
[smoke:manifest] assertWorm(reality_vault, update): STOPPAD (OK)
[smoke:manifest] assertSiloIsolation(kunskap → valv): STOPPAD (OK)
[smoke:manifest] PASS — manifest guards stop violations in isolated probes.

[smoke:valv-security] PASS — Valv WORM-session hardening.
```

## WORM

- `reality_vault` update blocked (`assertWorm` + `VaultService` via `assertArchitectureWrite`) — `scripts/smoke_manifest_violation.mjs:117-132`
- Manifest ↔ `firestore.rules` sample alignment: OK — collections `reality_vault`, `children_logs`, `journal`, `emotional_memory`, `evolution_ledger`, `kampspar` har `allow update, delete: if false` — `scripts/smoke_manifest_violation.mjs:86-106`
- Client guards wired: `assertWorm`, `assertSiloIsolation`, `guardedUpdateDoc`/`guardedDeleteDoc` — `src/modules/core/firebase/firestore.ts`, `src/modules/core/manifest/manifestGuards.ts:87-93`
- Allowed write paths: OK (create på WORM, update på mutable — ingen throw) — `scripts/smoke_manifest_violation.mjs:150-154`

## Tre silos (U1)

- `SILO_ISOLATED_DOMAINS`: K, V, F — `src/modules/core/manifest/domainContract.ts:71`
- K/V/F domains har `allowedCrossReads: []` — `src/modules/core/manifest/masterManifest.ts:17,55,76`
- `validateSiloIsolationManifest`: OK (K/V/F) — `scripts/smoke_manifest_violation.mjs:76-83`
- kunskap → valv cross-RAG: **STOPPAD** (`ManifestViolationError` SILO_ISOLATION) — `manifestGuards.ts:72-79`, smoke probe rad 134-147
- Backend mirror: `assertBackendSiloIsolation` i `functions/src/adk/registry.ts` + `orchestrator.ts` — statisk wiring OK via smoke

## Zero Footprint

- `vaultSessionLifecycle.ts` — `endVaultSession`, `clearClientCache`, `clearSpeglarSession`, server `invalidateSession` — `src/modules/core/security/vaultSessionLifecycle.ts:14-27`
- `useZeroFootprint.ts` — idle timeout (`VAULT_SESSION_IDLE_MS`), `visibilitychange`/`pagehide` med `closeDrawer: true` — `src/modules/core/auth/useZeroFootprint.ts:12-60`
- `VaultZoneGate` avmonterad (Fyren-gate kanon) — `scripts/smoke_valv_security.mjs:30-45`
- `signOutUser` anropar `endVaultSession` — `scripts/smoke_valv_security.mjs:145-148`
- Ingen kodändring — befintlig session/PIN-gate verifierad via smoke

## Plausible deniability (statisk)

Ej körd som smoke i P146 (utanför scope; körs i P147 `smoke:plausible-deniability`); statisk granskning mot `scripts/smoke_plausible_deniability.mjs`-mål:

- Fyren-gate (`openValvViaFyren`, WebAuthn) separerar Valv från dagbok — `valvFyrenGate.ts`
- Journal handoff utan auto-sync till `reality_vault` — `useJournalFlow.ts` saknar `saveVaultLog`/`reality_vault` (smoke mustNotInclude)
- `private_child` visibility i rules + client filter — `firestore.rules`, `firestore.ts`
- Bevis-routing kräver `assertVaultSession` — `functions/src/callables/valv.ts`, `inbox.ts`
- Dossier kräver Valv-unlock (`isOwnerVault`, `vaultUnlocked` storage) — `firestore.rules`, `storage.rules`

Baseline P144 rapporterade `smoke:plausible-deniability` **PASS** — inga regressioner observerade i statisk granskning.

## Sacred Features

- Verklighetsvalvet, Zero Footprint, tre silos — oförändrade i PMIR-zon
- MOD-WIDGET v3 (`WidgetModulerPage`/`Board`/`AddForm`) — `@locked`, `AuthGate variant="widget"`, ingen `assertArchitectureWrite`/cross-silo-bypass i moduler-widgeten — `src/modules/features/widgets/pages/WidgetModulerPage.tsx:1,20`
- Widget Valv-länk går via `NAV_PATHS.VALVET` + session-gate (`w1KompaktRailActions.ts:56`) — ingen direkt WORM-skrivning från moduler-route
- Android WH8 `ModulerWidgetProvider` — deep-link till widget-route; ingen Valv/WORM-API i provider (statisk granskning)

## PMIR-scan

Inga ändringar i förbjuden zon under P146.

| Resurs | Status |
|--------|--------|
| `firestore.rules` | ej rörd |
| `storage.rules` | ej rörd |
| `functions/src/sharedRules.ts` | ej rörd |
| `AppRoutes.tsx` | ej rörd |
| Hosting/rules/functions deploy | ej utförd |

## Kända info-GAP (ej blocker)

| ID | Beskrivning |
|----|-------------|
| P143-DEPLOY | Hosting deploy SKIP — väntar Pontus OK deploy |

## Verdict

**GO** — inga nya GAP identifierade i read-only audit. WORM, tre silos (K/V/F), Zero Footprint och plausible-deniability-skiljelinjer intakta. Startgate → P147 locked UX re-snapshot.
