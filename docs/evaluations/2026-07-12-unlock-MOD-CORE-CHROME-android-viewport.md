# Unlock MOD-CORE-CHROME — Android viewport

**Datum:** 2026-07-12  
approved: yes  
**Scope:** Android-scoped overflow/width fixes i dock-kanon-match.css — ingen header/dock layout-ändring.

## Tillåtna filer

- `src/styles/dock-kanon-match.css` (endast `html.platform-capacitor-android` block)

## Verifiering

`npm run smoke:basta-dock-lock` + `npm run smoke:android-viewport`
