# Archive

Do not commit personal Drive exports or IDE log files here.

- `drive-backup/` is gitignored (may contain family documents and PII).
- Use `docs/DRIVE_AUTOMATION.md` for the supported Drive → Cloud Functions pipeline.
